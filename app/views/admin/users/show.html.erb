<h1>管理者での「<%= @user.name %>」の詳細画面</h1>


<h3>投稿タスク一覧</h3>
  <table>
    <tr>
      <th>タイトル</th>
      <th>内容</th>
      <th>作成日</th>
      <th>終了期限</th>
      <th>ステータス</th>
      <th>優先順位</th>
    </tr>
  <% @user.tasks.each do |task| %>
    <tr class="task_row">
      <td><%= task.task_name %></td>
      <td><%= task.description %></td>
      <td><%= task.created_at %></td>
      <td><%= task.deadline %></td>
      <td><%= task.status %></td>
      <td><%= task.priority %></td>
    </tr>
  <% end %>
  </table>


<p>メールアドレス: <%= @user.email %></p>
<p>管理者権限: <%= @user.admin? ? 'あり' : 'なし' %></p>
<p><%= link_to '編集', edit_admin_user_path %></p>
<p><%= link_to '削除', [:admin, @user], method: :delete, data: { confirm: "ユーザ「#{@user.name}」を削除します。よろしいですか？" } %></p>

<%= link_to 'ユーザー一覧ページへ', admin_users_path%>